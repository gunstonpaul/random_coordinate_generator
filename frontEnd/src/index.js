import React, { useState } from 'react';
import ReactDOM from 'react-dom';
import Map from './map';


ReactDOM.render(
  <React.StrictMode>
    <Map />
  </React.StrictMode>,
  document.getElementById('root')
);
